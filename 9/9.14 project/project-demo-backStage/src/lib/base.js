const URL = 'http://localhost:80/public/index.php';
const IMGURL = 'http://localhost:80';
const SUCCESS = 200;
const ERROR = 404;

// export {URL, SUCCESS, ERROR}; // 使用时只能解构
export default {URL, IMGURL, SUCCESS, ERROR}; // 解不解构都行