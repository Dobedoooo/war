// let { arithmetic } = require('./arithmetic/');

// console.log(arithmetic(1, 2, '*'));

// const fs = require('fs');

// let config = JSON.parse(fs.readFileSync('./config.json').toString());

// console.log(config);

// 静态方法不会继承到实例上，只能用类来调用
